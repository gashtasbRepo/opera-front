import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  direction: "rtl",
  fonts: {
    body: 'shabnamThin, sans-serif',
    heading: 'shabnam, sans-serif',
  },
});

export default theme;